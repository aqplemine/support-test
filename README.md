# support-test

Simple game support chat widget that now sends messages to OpenAI using your own API key.

## Usage

1. Open `index.html` in your browser.
2. Click the 💬 button.
3. Enter your OpenAI API key and click **Save key**.
4. Send support questions in the chat.

The key is stored in browser `localStorage` on your machine.
